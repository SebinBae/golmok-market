spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/golmok
    username: golmok
    password: golmok
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  flyway:
    enabled: true
    locations: classpath:db/migration
